spring:
  application:
    name: splitfy-test
  datasource:
    url: jdbc:h2:mem:testdb;MODE=PostgreSQL;DATABASE_TO_LOWER=TRUE;DEFAULT_NULL_ORDERING=HIGH
    username: sa
    password:
    driver-class-name: org.h2.Driver
  jpa:
    hibernate:
      ddl-auto: create-drop
    properties:
      hibernate:
        dialect: org.hibernate.dialect.H2Dialect
    show-sql: true

logging:
  level:
    org.zzin.splitfy: DEBUG

jwt:
  issuer: splitfy-dev
  access-token-expiration: 30m
  secret: bm//miw3QerQuSJje/+pPl/LJmpIlICr0FZmVH2C2kY=
